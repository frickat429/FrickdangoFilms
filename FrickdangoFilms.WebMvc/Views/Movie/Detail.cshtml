@model FrickdangoFilms.Models.Movies.MovieDetailVM

@{
    string title = $"Movie {Model.Id}"; 
    ViewData["Title"] = title; 
}

<div class="d-flex justify-content-between align-items-center mb-2">
    <h1>@title</h1>
   
</div>